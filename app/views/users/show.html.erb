	<div class="continer">
	<div class="row">
		<div class="col-xs-3">
			<h2>User info</h2>
	<%= attachment_image_tag @user, :profile_image, :fill, 100,100, format: 'png',fallback: "no-image.png" %>
	<p>name</p>
     <%= @user.name %>
     <p>intorodaction</p>
     <%= @user.introduction %>
     <%= link_to "編集", edit_user_path(@user.id) %>
 </table>

<h1>New book</h1>
<%= form_for (@book) do|f| %>
<h4>Title</h4>
	<%= f.text_field :title %>
	<h4>body</h4>
	<%= f.text_area :body %>
	<%= f.submit 'Create Book' %>
	<% end %>
</div>

			<div class="col-xs-9">
			<h1>Bookes</h1>
				<table>
					<tr>
						<th></th>
						<th>Title</th>
						<th>Opnion</th>
					</tr>
					<% @books.each do |book| %>
					<tr>
						<td><%= attachment_image_tag @user, :profile_image, :fill, 100,100, format: 'png',fallback: "no-image.png" %> </td>
						<td><%= book.title %></td>
						<td><%= book.body %></td>
					</tr>
					<% end %>
			</table>
		</div>
	</div>