<div class="container">
	<div class="row">
		<div class="col-xs-3">
		<h2>User info</h2>
		<%= attachment_image_tag current_user, :profile_image, :fill, 100,100, format: 'png',fallback: "no-image.png" %>
     <p>name <%= current_user.name %>
</p>
    <p>intorodaction <%= current_user.introduction %></p>
     <%= link_to "編集", edit_user_path(current_user.id) %>
<h1>New book</h1>
<%= form_for (@book) do|f| %>
<% if @book.errors.any? %>
	<h3><%= @book.errors.count %>errors prohibited this obj from being saved:</h3>
	  <% @book.errors.full_messages.each do |message| %>
	  <%= message %>
	  <% end %>
	  <% end %>
<h4>title</h4>
	<%= f.text_field :title %>
	<h4>body</h4>
	<%= f.text_area :body %>
	<%= f.submit 'Create Book' %>
	<% end %>
</div>
<div class="col-xs-9">
<h1>Bookes</h1>
		<table class="bookes_teble">
				<thead>
				<tr>
					<th></th>
					<th>Title</th>
					<th>Opnion</th>
				</tr>
			</thead>
				<tbody>
				<% @books.each do |book| %>
				<tr>
					<td><%= link_to (book.user) do %>
						<%= attachment_image_tag book.user,:profile_image, format: 'png',fallback: "no-image.png",size: "50x50" %>
							<% end %>
						</td>
					<td><%= link_to book.title ,book%></td>
					<td><%= book.body %></td>
				</tr>
				<% end %>
			</tbody>
		</table>
</div>
</div>
</div>