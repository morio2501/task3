
<header>
		<h1>Bookes</h1>
		<table>
				<tr>
					<th>Title</th>
					<th>Opnion</th>
				</tr>
				<% @books.each do |book| %>
				<tr>
					<td><%= book.title %></td>
					<tb><%= book.body %></tb>
				</tr>
				<% end %>
		</table>
		
<h1>New book</h1>
<%= form_for (@book) do|f| %>
<h4>title</h4>
	<%= f.text_field :title %>
	<h4>body</h4>
	<%= f.text_area :body %>
	<%= f.submit 'Create Book' %>
	<% end %>