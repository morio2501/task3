<div class="container">
	<div class="row">
		<div class="col-xs-3">
	<h2>User info</h2>
<%= attachment_image_tag current_user, :profile_image, :fill, 100,100, format: 'png',fallback: "no-image.png" %>
	<p>name</p>
     <%= current_user.name %>
     <p>intorodaction</p>
     <%= current_user.introduction %>
     <%= link_to "編集", edit_user_path(current_user.id) %>


<h1>New book</h1>
<%= form_for (@book) do|f| %>
<h4>title</h4>
	<%= f.text_field :title %>
	<h4>body</h4>
	<%= f.text_area :body %>
	<%= f.submit 'Create Book' %>
	<% end %>
</div>

<div class="col-xs-9">
<h1>Book detail</h1>
	<%= link_to @user do %>
	<%= attachment_image_tag @user, :profile_image, :fill, 100,100, format: 'png',fallback: "no-image.png" %>
	<%= @user.name %>
	<% end %>
     <%= link_to @book2.title,@book2 %>
     <%= @book2.body %>

    <% if @book2.user == current_user %>
<%= link_to 'Edit', edit_book_path(@book2.id) %>
<%= link_to "Destroy", book_path(@book2.id), method: :delete, data: {confirm: 'Are you sure?'} %>
<% end %>
</div>
</div>
</div>
