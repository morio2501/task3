<div class="bookdetail">
<h1>Book detail</h1>
	<%= attachment_image_tag @user, :profile_image, fallback: "no-image.png" %>
     <%= @book2.title %>
     <%= @book2.body %>

    <% if @book2.user == current_user %>
<%= link_to 'edit', edit_book_path(@book2.id) %>
<%= link_to "Destroy", book_path(@book2.id), method: :delete, data: {confirm: 'Are you sure?'} %>
<% end %>
</div>


<%= attachment_image_tag current_user, :profile_image, fallback: "no-image.png" %>
	<p>name</p>
     <%= current_user.name %>
     <p>intorodaction</p>
     <%= current_user.introduction %>
     <%= link_to "編集", edit_user_path(current_user.id) %>


<h1>New book</h1>
<%= form_for (@book) do|f| %>
<h4>title</h4>
	<%= f.text_field :title %>
	<h4>body</h4>
	<%= f.text_area :body %>
	<%= f.submit 'Create Book' %>
	<% end %>
